---
name: brush-up-skill
description: スキル使用後のやり取りを基にスキルを改善・更新する。使用したスキルを毎回ブラッシュアップしたいとき、スキル改善・更新・修正時に使用。 Use when this capability is needed.
metadata:
  author: hakushun
---

# スキルブラッシュアップスキル

スキルを使用した後のやり取りから学び、そのスキルを改善・更新するワークフロー。使用するたびにスキルを磨いていくために使う。

## 実行手順

### 1. 対象スキルの特定

- **引数がある場合**: `$ARGUMENTS` またはユーザーが指定したスキル名を対象とする
- **引数がない場合**:
  - 現在の会話で参照・使用されたスキルを特定する
  - 複数ある場合はユーザーに「どのスキルをブラッシュアップしますか？」と確認する

### 2. スキルファイルの読み込み

対象スキルのファイルを特定し、すべて読み込む。

**プロジェクトスキルの場所（本リポジトリ）:**

```
.claude/skills/[スキル名]/
```

**読み込むファイル:**
- `SKILL.md`（必須）
- `references/` 配下のファイル（存在する場合）
- `templates/` 配下のファイル（存在する場合）
- その他のサポートファイル（agents/, 同階層の .md など）

`Glob` や `LS` でディレクトリ構成を確認してから `Read` で内容を取得する。

### 3. 会話からの改善点の抽出

現在の会話を分析し、以下の観点で改善点を抽出する。

**機能面**
- スキルが期待通りに動作しなかった箇所
- 不足していた手順やステップ
- 追加すべき機能や処理

**説明面**
- 曖昧だった指示や説明
- 誤解を招いた表現
- 不足していたコンテキスト

**ユーザビリティ面**
- 使いにくかった操作フロー
- 改善すべきテンプレートやフォーマット
- より良い引数・トリガーの扱い方

### 4. 改善案の提示

[templates/improvement-report.md](templates/improvement-report.md) の形式で改善案を提示する。

**提示内容:**
- 現在のスキルの問題点（会話から抽出した事実）
- 具体的な改善案（変更案）
- 変更前後の差分プレビュー（該当箇所のみでよい）

### 5. ユーザーの確認

改善案についてユーザーの確認を得る。

- 提案通り更新してよいか
- 一部のみ採用するか
- 追加の修正が必要か

### 6. スキルの更新

ユーザーの承認後、スキルファイルを更新する。

1. 対象ファイルを `StrReplace` または `Write` で編集
2. 新規ファイルが必要な場合は作成
3. 不要になったファイルがあれば削除を提案（ユーザー確認の上で実行）

### 7. 更新完了報告

更新した内容を簡潔に報告する。

- 変更したファイル一覧
- 主な変更点のサマリー
- 次回からスキル利用時に確認してほしい点（任意）

## 注意事項

- **会話コンテキストを必ず参照する**: このスキルは会話の中での気づきを基に改善する
- **最小限の変更に留める**: 必要な改善のみ行い、過剰な変更は避ける
- **ユーザーの意図を確認する**: 不明な点は必ずユーザーに確認する
- **大きな変更前**: 元のファイルの内容を読み直し、意図しない改変を防ぐ

## 参考

- 改善レポートテンプレート: [templates/improvement-report.md](templates/improvement-report.md)

---
> Converted and distributed by [TomeVault](https://tomevault.io/claim/hakushun) — claim your Tome and manage your conversions.
<!-- tomevault:4.0:skill_md:2026-04-14 -->
